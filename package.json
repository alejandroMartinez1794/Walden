{
  "name": "basileia",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postbuild": "cd backend && npm ci --only=production"
  },
  "dependencies": {
    "react-calendar": "^5.1.0",
    "react-icons": "^5.1.0",
    "spinner": "^0.3.4",
    "winston": "^3.19.0",
    "winston-daily-rotate-file": "^5.0.0"
  }
}
