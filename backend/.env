## This file is tracked only as a template. Copy it to `backend/.env.local` and replace placeholders with your secrets.
## `.env.local` is ignored by Git and overrides the values below, so keep your actual values there.

PORT=5000
MONGO_URL=mongodb+srv://lamartinezf:Lamf1794_@cluster1.rl8qvy2.mongodb.net/medicare?retryWrites=true&w=majority
JWT_SECRET_KEY=your_jwt_secret_here
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:5000/api/v1/calendar/google/callback
FRONTEND_BASE_URL=http://localhost:5173