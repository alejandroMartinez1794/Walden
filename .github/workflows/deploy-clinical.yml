name: Clinical Deploy

on:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Manual deployment only
        run: |
          echo "⚠️  Clinical Deploy is manual only (workflow_dispatch)"
          echo "Use GitHub UI or CLI to trigger manually"
